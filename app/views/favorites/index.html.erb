<h1>Favorites Pictures</h1>

<table>
  <thead>
    <tr>
      <th>Content</th>
      <th>Image</th>
      <th>User</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @favorite.each do |favorite| %>
      <td><%= favorite.picture.content unless favorite.picture.content == nil %></td>
      <td><%= image_tag favorite.picture.image.url, size: '100x100' unless favorite.picture.image == nil %></td>
      <td><%= favorite.picture.user.name unless favorite.picture.user.name == nil  %></td>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Pictures', pictures_path %>
