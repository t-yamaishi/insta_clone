<h1><%= @user.name %>のページ</h1>

<p>メールアドレス: <%= @user.email %></p>

<p>
  <% unless @user.image.url == nil %>
  <strong>Image:</strong>
  <%= image_tag @user.image.url, size: '100x100' %>
  <% end %>
</p>

<td><%= link_to 'ProfileEdit',edit_user_path if @user.id == current_user.id %></td>

<p><%= link_to '投稿一覧へ', pictures_path %></p>
